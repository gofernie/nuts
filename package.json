{
  "name": "proto-photon",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "cross-env ASTRO_DISABLE_WASM=true astro dev --host --https --cert certs/localhost.pem --key certs/localhost-key.pem --port 4321",
    "dev:http": "cross-env ASTRO_DISABLE_WASM=true astro dev --host --port 4321",
    "build": "cross-env ASTRO_DISABLE_WASM=true astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.14.0",
    "@astrojs/netlify": "^6.6.0",
    "@astrojs/sitemap": "^3.6.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@astrojs/compiler": "^2.10.3",
    "@astrojs/react": "^4.4.0",
    "@vitejs/plugin-basic-ssl": "^2.1.0",
    "cross-env": "^10.1.0",
    "netlify-cli": "^23.9.1"
  }
}
